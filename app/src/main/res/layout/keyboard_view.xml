<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:orientation="vertical">

    <View
        android:layout_width="match_parent"
        android:layout_height="10dp"
        android:background="@drawable/keyboard_shadow" />

    <droidfreak.com.simplekeyboard.view.SimpleKeyboardView
        android:id="@+id/keyboard_view"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="#ffffff"
        android:keyBackground="@drawable/key_bg"
        android:keyPreviewLayout="@layout/preview_indication"
        android:keyTextColor="@color/text_colors"
        android:shadowColor="#ffffff"
        android:shadowRadius="0"/>

</LinearLayout>